/** @type {import('next').NextConfig} */
const nextConfig = {
  // Statický web — žiadne serverové funkcie sú potrebné.
  // Vercel automaticky rozpozná Next.js, build prebehne bez ďalšej konfigurácie.
  reactStrictMode: true,
};

export default nextConfig;
